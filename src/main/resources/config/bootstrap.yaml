# Default Optum ECCSM Vault Configuration.
spring:
  cloud.vault:
    enabled: false

logging:
  level:
    org:
      springframework:
        boot:
          docker:
            compose:
              lifecycle: trace
